# sim/ — Recompilable sim-core slice (Product 1)

This directory holds the **functional re-implementation in C++** of Empire at War's simulation
core, lifted from `StarWarsG.exe` per `openspec/engine/decomp_plan.md` Phase 3. It is **not** a
byte-matching decomp — it reproduces *behavior*, validated by **differential testing against the
live binary** (the winmm hook DLL captures ground-truth `DIFFTRACE` per-tick checksums; the lifted
code must reproduce the same per-tick sim state).

## Methodology (per decomp_plan.md §1)

- Lift in **dependency order**: tick clock → entity-update spine → locomotor integrators →
  hardpoint fire-control → the IN sim behaviors.
- Each unit is a clean, idiomatic C++ re-implementation. Original RVAs and struct offsets are kept
  in comments for traceability (`// <- FUN_14027c360 +0x14`), but the code does **not** mirror the
  decompiler's variable soup.
- Each unit ships a **host-side self-test** (`sim/tests/`) that replays a deterministic input
  stream and asserts the lifted behavior. The in-game `DIFFTRACE` stream is the end-to-end oracle;
  the host test is the fast inner loop.
- A unit is "lifted" only when its semantics are reproduced **and** its determinism-relevant
  behavior (ordering, fixed-point/float accumulation, RNG, lockstep gating) is called out explicitly.

## Build

Host validation (no game needed):
```
just sim-test            # compiles sim/ + runs sim/tests/*; all must pass
```
or directly:
```
nix develop --command g++ -std=c++17 -O2 -Wall -Isim sim/tests/tick_clock_test.cpp sim/tick_clock.cpp -o /tmp/tick_clock_test && /tmp/tick_clock_test
```

## Status

| Unit | Source RVAs | State | Determinism notes |
|---|---|---|---|
| **tick clock / scheduler** | `27c360` `27c2f0` `27c330` `27c5f0` | ✅ lifted (SP); MP lockstep gate behind `IFrameSynchronizer` (Phase-4 dep) | Fixed-timestep `1000/cap` ms; MP = lockstep `MaxAhead` + per-player command-ready gate |
| **entity-update spine** | `3639d0` `2be640` `3a6b80` | ✅ lifted (skeleton + hooks) | reverse-order behavior dispatch; GOM list order; **no child recursion** (flat list); per-unit bodies are `EntityUpdateHooks` |
| **locomotor integrators** | Starship `6236b0`+`6224b0` (+helpers); SimpleSpace `626420`+`41c000` | ✅ Starship skeleton + integrator lifted; SimpleSpace dispatch skeleton + direction/straight-move lifted (Hermite mover `625990` + special-mode `6269f0` are hooks); shared model + primitives | **✅ FIRST IN-GAME ORACLE PASS**: SimpleSpace `dir=(cos(hd·DEG2RAD),sin(hd·DEG2RAD))` matches live `state+0x14/18/1c` bit-exact (182 ticks) + `disp∥dir` (150/150). Two families (Starship raw-velocity vs SimpleSpace facing+spline-magnitude). Magnitude movers still to lift |
| hardpoint fire-control | `a76b0` `387f50` `540070` `387010` (+ `396df0`/`396070`/`387400` hooks) | 🟡 fire-budget distribution + consume + per-mount dispatcher skeleton lifted (host-validated); ship scalars + target-acq (`387400`) behind `HardpointEnv` | **`total_w` cancels** → `share_i = budget_i·(capacity−base)`; the game-speed weight scale (`540070`) is irrelevant to every share. **DTFIRE in-game survey: phase-1 distribution is DORMANT in TR content** — over a 114k-tick capital battle (81,302 ship-ticks with ≥2 active mounts) the distribution branch consumed budget on **0** ship-ticks; summed weight stays sub-1e-6 (`maxtw` rounds to 0 all battle). Weapons fire via phase-2 dispatch (`387010`→`387400`); the cancellation oracle is unobservable in this mod. **`387400` target-acq is the live fire path**. **✅ its determinism-critical OPPORTUNITY-TARGET SCAN core now lifted** (`scan_opportunity_target`, host-validated; random-start circular player sweep consuming `SimRng`, with the fog-stall + score-leak + skip-self subtleties faithful) — RNG seeding already in-game-validated via DTRNG. The surrounding guard cascade + command emission stay behind `OppScanEnv`/`HardpointEnv::acquire_targets` |
| **global sim RNG** | `1ffb40` `1ffbb0` `1ffdb0` (LCG state `DAT_140a13e24`) | ✅ lifted (`SimRng`: `range_i`/`range_f`/`percent`) | **✅ IN-GAME ORACLE PASS** (DTRNG, 20000/20000 ret+state bit-exact; draw-counts 1–7 reject + a==b no-draw): LCG `s'=s·0x41c64e6d+0xbdf`, output `(s>>10)&0x7fff`. THE Phase-3/4 determinism primitive (202 callers). Draw COUNT per call (rejection loops) is lockstep-relevant. `387400`'s opportunity-scan seeds its start index here |
| sim behaviors (13 IN) | see decomp_plan census; **#1 `DamageTracking 0x58bd80`** (`sim/damage_tracking.{h,cpp}`), **#2 `EnergyPool 0x56c030`** (`sim/energy_pool.{h,cpp}`), **#3 `AbilityCountdown 0x42f910`** (`sim/ability_countdown.{h,cpp}`), **#4 `UnitAI 0x4f6070`** (`sim/unit_ai.{h,cpp}`), **#5 `Nebula 0x437b60`** (`sim/nebula.{h,cpp}`), **#6 `AsteroidFieldDamage 0x437310`** (`sim/asteroid_field_damage.{h,cpp}`), **#7 `Targeting 0x633a30` DISPATCHER** (`sim/targeting_dispatch.{h,cpp}`, part 1 — sub-bodies deferred) | 🟡 **6/13 lifted + #7 dispatcher** (host-validated; DamageTracking 7 cases, EnergyPool 10 cases, AbilityCountdown 8 cases, UnitAI 9 cases, Nebula 10 cases, AsteroidFieldDamage 5 cases, Targeting-dispatcher 4 cases) | **#1 census correction**: DamageTracking is *not* HP-application — it's a **timed status-effect decay ticker** over the entity's own `+0x1a0` list; only cross-boundary act is the edge-triggered `0x2d` emit on empty (first real driver of the channel-1 `CommandSink` seam). World-coupled decay rate behind `DamageEnv`; insertion-ordered vector faithful (the single emit is removal-order independent). **#2 EnergyPool** — **✅ DTNRG IN-GAME ORACLE PASS**: a **pure self-contained Phase-A regen ticker** — `value' = clamp(value + rate·dt, 0, max)` on its own pool `(entity+0xf0)+0xf8`; NO sim RNG, NO cross-entity read/write, NO CommandSink (contrast #1). Drain gate (`template+0x8b` source + predicate) → force-zero / regen / bail; world-coupled `dt`(`398010` game-speed scale) + `max`(`372320` base×(1+Σbuffs)) + drain disposition behind `EnergyEnv`. **Oracle (67 latched + 58 interior samples, capital battle): recovered `dt`=1.000000 exact (single shared game-speed scalar) across all interior samples; model reproduces va 67/67 via the upper-clamp.** Lower-clamp / disabled / drain edges host-covered but in-game dormant (no unit ran dry / ionized / drained). Energy pools regen every combat tick → no DamageTracking-style dormancy on the main path. **#3 AbilityCountdown** — self-contained **INTEGER-tick** cooldown/chargeup ticker over the entity's own `entity+0x1e8` 77-slot recharge manager (timer `+0x8+i*4`, chargeup target `+0x13c+i*4`, mode byte `+0x270+i`, last-tick `+0x2c0`); `delta=tick−last_tick` (no game-speed float scale — cleaner than #2's `dt`). Two clamped modes: COUNTDOWN `timer=max(timer−delta,0)`, on the **edge** it reaches 0 emits `0x2c` AbilityRecharged (gated by the heavy `39b480` availability predicate, behind `AbilityCountdownEnv::ability_ready`); CHARGEUP `timer=min(timer+delta,target)`, on reaching target calls `42f460` (which emits `0x29` + an SFX cue + RECYCLES the slot into a cooldown: mode flips 1→0, timer reset to a new larger value, target updated — env-modelled). No sim RNG; Phase-A parallel-safe modulo the two buffered emits. **✅ DTABIL IN-GAME ORACLE PASS** (capital battle, 25 entities): pure-core ta reproduced **19981/19981 bit-exact** (both modes), 19 charge-complete recycle events validated (trigger correct; recycle env-owned), **13 cooldown→0 edges** confirm the `0x2c` emit trigger. Both cross-boundary acts exercised in-game (richer than #1, whose `0x2d` stayed dormant). **#4 UnitAI** — the **fog-of-war / sensor-visibility ticker** over the entity's AI manager `owner+0x108`, and the **first CROSS-ENTITY behavior** (reads the global object list, writes a per-(observer,object) visibility matrix). Three blocks: (A) 300-tick-throttled self-refresh — when the observer's 12-float world transform (`obj+0x248..+0x274`) changed vs the cache (`ai+0x68..+0x94`), push a "moved" update to the shared sensor grid + recache; fixed-phase scheduler (`next_due+=300`, not `now+300`); (B) gated reveal hook (`4f6470`); (C) visibility matrix — for each global object, evaluate a heavy LOS/faction predicate (`35f470`, env) and maintain an owner-private bit (`ai+0x58[idx]`); on each bit EDGE notify the shared grid (`657ce0` became-visible / `657db0` became-hidden / `659760` moved). **KEY: NOT Phase-A parallel-safe** — unlike #1–3 (own-entity state only), its three cross-boundary acts mutate the **shared** sensor grid `DAT_140b31900` keyed by (observer,object); to parallelise, buffer those writes (CommandSink-style) or shard the grid per-observer. The bit array itself is owner-private/safe. 9 host cases (throttle fixed-phase + change-detect, init-once, visibility edges, sensor-absent suppresses notifies but still sets bits, fog-off clears, null-skip + count-guard, reveal gate). **✅ DTUAI IN-GAME ORACLE PASS** (capital battle, 333 distinct observers, 6988 samples): throttle reproduced **3427/3427 advances exactly `+300`** (fixed-phase, due & sensor-present); transform-cache post-condition `cacheok==1` on **6988/6988**; 90 visibility edges (36 became-visible / 54 became-hidden) characterised as the shared-grid notify count; bounds clean. Deterministic core (throttle + transform cache) bit-exact; vis-matrix bit VALUE is the env LOS predicate (characterised, not reproduced). Capture: `logs/dtuai_oracle_pass.log`. **#5 Nebula** — the "clean float integrator": STAGE 1 (unconditional every tick) is a **semi-implicit damped harmonic oscillator** ramping the nebula-effect intensity `value` (`sub+0x11c`, `sub=*(entity+0xf0)`) toward `equilibrium` (`sub+0x120`): `w=freq·2`, `inv=1/(1+wdt+0.48·wdt²+0.235·wdt³)`, `value'=(tmp+dx)·inv+eq`, `velocity'=(velocity−tmp·w)·inv` (`tmp=(dx·w+velocity)·dt`, `dx=value−eq`) — pure float, op order preserved bit-for-bit (constants `2.0`/`0.48`/`0.235`/`1.0`; `dt=DAT_140b0a344`). STAGE 2 is a membership SM gated by a **time-based LINGER throttle** (`380bb0`: skip the spatial scan while `(now−enter_tick)/30 < grace`); on a sphere-scan hit it edge-fires the **enter** hook (`4376f0`, emits `0x2b` to disable nebula-affected abilities) + `eq→1`, else the **leave** hook (`39fb40`, emits `0x2c` re-enable) + `eq→0`; `enter_tick=sub+0x104`. **THREADING: cross-entity coupling is READ-ONLY** (scan reads the global set + other objects' template flags but writes only OWN sub-object state) → closer to Phase-A-safe than #4. 10 host cases (spring bit-exact vs reference, relaxation, `freq=0` linear-glide, enter/leave/no-edge, linger-pin vs already-full no-op, stage ordering). **✅ DTNEB IN-GAME ORACLE PASS** (nebula battle, 133 logged entities, 614503 entity-ticks): spring `value'`/`velocity'` reproduced **614503/614503 BIT-EXACT** (`spring_bad=0`; analyzer confirms 20000/20000 logged in float32) through the full ramp; **308 enters / 218 leaves** (all `eq→1`/`eq→0` correct); `dt` a single per-frame `1/30` global; LINGER pins **0** (the grace-window throttle is dormant in TR — every in-nebula tick took the full scan — so the pin path is host-covered only, like #1's `0x2d` emit). Capture: `logs/dtneb_oracle_pass.log`. **Captured with the new `EAW_ORACLE` build** (`just build-winmm-oracle` — DT* oracle hooks only, no timing-profiler body-patchers, which fault on battle load: `c0000005` in `b375380`). **#6 AsteroidFieldDamage** — Nebula's **sibling** (adjacent RVA `0x437310`, same `entity+0xf0` sub-object + a last-event-tick field `+0x10c` mirroring Nebula's `+0x104`): a **probabilistic per-tick collision-damage trigger** for ships sitting in an asteroid field. GATE = game mode ∈ {1,2,0x20} (`397900`) + a unit predicate chain + both config scalars non-zero (probability `DAT_140b16d64`, amount `DAT_140b16d60`). SCAN = a sphere query (`5d7c00`) over the global object set; for each in-range object carrying the asteroid-field flag (`object->template+0x298` deref, byte `+0x54`): draw `roll=rng_uniform(0,1)` (one `1ffbb0` sim-LCG draw), apply collision damage if `roll ≤ probability` (`436920`), and stamp `sub+0x10c=now`; if none found, `sub+0x10c=-1`. apply damages the SHIP ITSELF at a randomly-chosen hardpoint (drawing a 2nd float + an int roll) and spawns an impact-fx entity into the GOM. **THREADING: NOT Phase-A-safe, but for a new reason** — unlike UnitAI #4 (foreign gameplay write to the shared sensor grid), the gameplay write here lands on SELF; the hazards are (a) the **shared sim-RNG seed `DAT_140a13e24` is advanced a membership-dependent number of times** (so draw ORDER across entities must serialise — the sim_rng.h determinism contract) and (b) apply spawns into the global GOM. First IN behavior whose non-safety is purely the RNG + GOM-spawn seam (both Phase-B). 5 host cases (gate roll bit-exact vs reference LCG + single advance, `roll≤prob` apply boundaries, proximity stamp now/-1, full-tick draw count == in-range asteroid count, inactive tick = no-op leaving RNG + state untouched). **✅ DTAST IN-GAME ORACLE PASS** (asteroid-field battle, 10 entities, 30542 entity-ticks / 5886 active in-field): **proximity biconditional `(seed advanced) ⟺ (sub+0x10c == now)` held 5906/5906** (`bicond_bad=0` — the `+0x10c` SM and RNG consumption agree exactly); **LCG recurrence reproduced 5886/5886** advanced ticks (`s'=s·0x41c64e6d+0xbdf` forward-stepped to `seed_after`, **0** k-mismatches vs the hook's draw count); **first gate roll `range_f(0,1)` bit-exact 5886/5886** (float32, 0 out of `[0,1]`); **per-tick draw count `k=1..13` is membership-dependent** (the determinism hazard, exhibited directly); predicted applies ≈ **1252/5886 = 21.3% ≈ prob 0.2**. Capture: `logs/dtast_oracle_pass.log`. Captured with the `EAW_ORACLE` build. **#7 Targeting (DISPATCHER, part 1)** — the first non-ticker: Targeting is the AI **fire-control orchestrator**, a ~5500-byte body split across two game-mode sub-implementations (`634810` mode 1 / `633ae0` mode 2). Per the incremental plan, only the thin **dispatcher** `0x633a30` is lifted here; the two sub-bodies (live global-set reads + GOM target-assign/move/fire emission, UnitAI #4 seam-class) are **deferred**. The dispatcher resolves a mode int — controller (`entity->get_component(1)`, vtbl `+0x10`) present → `controller->control_mode()` (vtbl `+0x238`, same accessor `397900` reads); else `DAT_140b15418` present → game-speed mode (vtbl `+0xe0`) : default `DAT_140b153fc` — then routes mode 1 → individual, 2 → team, else NoOp `0x650000`. Core is a pure side-effect-free integer decision (no RNG/writes/cross-entity reads) → **trivially Phase-A-safe**; Targeting's threading character lives entirely in the deferred sub-bodies. 4 host cases (source-selection order, dispatch mapping over 1/2/else, end-to-end on every source, controller-NoOp does not fall through). **✅ DTARG IN-GAME ORACLE PASS** (space battle, 8 entities, 20000 targeting-entity-ticks): validated as a BICONDITIONAL `predicted == actual` — the hook replicates the resolution (PREDICTED impl) and two entry markers on `634810`/`633ae0` give the ACTUAL impl that ran. **Dispatch biconditional `pred==act` held 20000/20000** (`bicond_bad=0`), mutual exclusion clean (`multi=0`, `read_ok` failures=0), resolution-consistency mismatches=0. Every tick resolved via the **controller** source reporting **mode 2 → team targeting (`633ae0`)**; mode-1 (individual `634810`), NoOp, and manager/default fallback were **dormant** in this battle (host-covered only, like #1's `0x2d` emit / #5's LINGER pins), oracle stays armed. Decisive for the dominant in-combat path. Capture: `logs/dtarg_oracle_pass.log`. |
| command/event queue | `058570` `220ed0`/`e90`/`eb0` `240940` `1cacb0` (signal/slot); `2d5290`/`2d5440`/`2d72c0` SFXEventManager (presentation cue) | 🟢 **BOTH channels lifted** — channel-1 dispatch fan-out (`240940`, `sim/signal_dispatch.{h,cpp}`) + channel-2 SFX cue (`sim/sfx_channel.{h,cpp}`), both host-validated; both `387400` emitters wired (`CommandSink`) | **Two channels w/ OPPOSITE determinism weight**: (1) per-object signal/slot — each GameObject embeds a `SignalDispatcherClass` at `+0x38` (hash-map `sig_id→SignalListClass`, insertion-ordered slots; emit walks head→tail); order IS lockstep-critical. (2) **presentation SFX cue** — `387400:99` is `FUN_1402d5290`=`SFXEventManagerClass::Start_SFX_Event` (NOT a gameplay scheduled queue, as earlier mislabeled), order is NOT lockstep-relevant. `387400` emits 2 signals, now **both lifted & wired to `sim::CommandSink`** (`sim/recording_command_sink.h` is the reusable Phase-A sink); the two are **mutually exclusive within a call** (the `cVar6` gate at line 221 / the `state_flag` return at line 203) — `387400` emits EITHER 0x20 OR (via the tail) 0x21, never both. Emission decisions are deterministic given unit inputs & slot-independent → recording `CommandSink` is a faithful Phase-A seam. Canonical Phase-B apply order = per-emission insertion order × GOM tick order. **✅ `acquire_opportunity_target` (lines 220–316, the `scan_opportunity_target` *caller*) → sig 0x21**: the 4 emission outcomes (keep-existing / emit-after-rescan / bound-not-fire-allowed→clear / emitter-bail) + the rescan-stamp comma side effect host-validated. **✅ `commit_ordered_fire` (lines 173–201) → sig 0x20** (parameterless FireOrderInProgress): the in_progress_flag 0→1 transition (ordered & regular branches, edge-only emit, not-committed clear) host-validated, incl. the distinguishing **flag-persists-on-emitter-bail** subtlety (0x20 sets the flag *before* resolving the emitter and bails via `goto`, so the transition survives an emit suppression — unlike 0x21's full-`return` bail). **✅ Phase-B `SignalDispatcher::emit` = `FUN_140240940` LIFTED** (`sim/signal_dispatch.{h,cpp}`) — the synchronous observer fan-out the emit seam drains into: insertion-order slot firing, the **lockstep-cursor dedup** (a `fired` vector + monotonic cursor, NOT a set — collapses only the already-fired prefix on a restart), the **touched-flag restart** (any connect/disconnect/has-slot during dispatch trips `+0x08`; the walk re-runs from head skipping fired slots & firing appended ones; left set after a restart so a nested/outer dispatch also restarts = the `uVar9=1` propagation), `+0x0c` depth bookkeeping. `drain_commands` = serial Phase-B apply: replays a `RecordingCommandSink` buffer in canonical order, resolving each `emitter+0x38` & routing the 0x21 payload. 9 host cases. **✅ CHANNEL-2 = PRESENTATION SFX CUE LIFTED** (`sim/sfx_channel.{h,cpp}`) — **correction of the earlier "scheduled-event queue" label**: `387400:99`'s second emission is `FUN_1402d5290(&DAT_140b27e60, DAT_140b301c8, …)` = `SFXEventManagerClass::Start_SFX_Event_Internally` (decomp `2d5440`: ActiveSFXEventClass, presets, loops, voice limits) — a sound cue on ordered-fire completion. **Presentation-only, fully decoupled from lockstep**: (a) its probability gate (`2d5440:153`) draws from `&DAT_140a13e20`, a SEPARATE LCG word from the sim RNG `&DAT_140a13e24` (corpus-wide: `a13e24`=core sim fns only; `a13e20`=audio only, independently seeded `5d990:95`); (b) its internal queue (`2d72c0`) sorts by **wall-clock `timeGetTime()`** — a lockstep queue can't key on real time. So ordering is NOT lockstep-relevant (OPPOSITE of channel-1): the drain may reorder/drop/run on a presentation thread with zero sim effect. `CommandSink::emit_sfx_event`→`RecordingCommandSink.sfx`→`drain_sfx(buffer, SfxSink&)` (presentation boundary; real impl calls Start_SFX_Event). 4 host cases incl. the **sim-RNG-untouched invariant** (a `SimRng` is bit-identical across a drain). The genuine `OutgoingEventQueueClass` (`b2ed18`, "OutgoingEventQueueClass::Add", `(fire_time,insertion)`) is a **galactic-layer** MP queue (`28d400` PENDING_TACTICAL_BATTLE_VOTE) — outside this tactical slice; `387400` never touches it |

The tick clock stays **serial** in the parallel rewrite — the parallelism is *within* a tick, over
entities (Phase A), not across ticks. Lifting it first pins the tick that every other unit is
validated against.
